`hello` directory contains a sample custom command that just says "hello".

`build` directory contains `build.yaml` for the `build_sensorbee` command. The config adds the new `hello` command to `sensorbee`.
